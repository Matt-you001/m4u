/*import { setGlobalOptions } from "firebase-functions/v2";
import app from "./index.js";

setGlobalOptions({
  maxInstances: 10,
  region: "us-central1",
});

export const api = onRequest(
  {
    cors: true,
    region: "us-central1",
    secrets: [  "OPENAI_API_KEY", "JWT_SECRET", "DB_URL"],
  },
  app
);*/
